@model CarServ.MVC.Models.Service

@{
    ViewData["Title"] = Model.ServiceName;
    var relatedServices = ViewData["RelatedServices"] as List<CarServ.MVC.Models.Service> ?? new List<CarServ.MVC.Models.Service>();
}

<!-- Page Header Start -->
<div class="container-fluid page-header mb-5 p-0" style="background-image: url(/img/carousel-bg-1.jpg);">
    <div class="container-fluid page-header-inner py-5">
        <div class="container text-center pb-5">
            <h1 class="display-3 text-white mb-3 animated slideInDown">@Model.ServiceName</h1>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb justify-content-center text-uppercase">
                    <li class="breadcrumb-item"><a href="@Url.Action("Index", "Home")">Trang Chủ</a></li>
                    <li class="breadcrumb-item"><a href="@Url.Action("Index", "Service")">Dịch Vụ</a></li>
                    <li class="breadcrumb-item text-white active" aria-current="page">@Model.ServiceName</li>
                </ol>
            </nav>
        </div>
    </div>
</div>
<!-- Page Header End -->

<!-- Service Details Start -->
<div class="container-xxl py-5">
    <div class="container">
        <div class="row g-5">
            <div class="col-lg-8">
                <div class="mb-5">
                    @if (!string.IsNullOrEmpty(Model.ImageUrl))
                    {
                        <img class="img-fluid rounded w-100 mb-4" src="@Model.ImageUrl" alt="@Model.ServiceName" style="height: 400px; object-fit: cover;">
                    }
                    else
                    {
                        <img class="img-fluid rounded w-100 mb-4" src="~/img/service-1.jpg" alt="@Model.ServiceName" style="height: 400px; object-fit: cover;">
                    }

                    <h2 class="mb-4">@Model.ServiceName</h2>
                    
                    @if (!string.IsNullOrEmpty(Model.Description))
                    {
                        <div class="mb-4">
                            @Html.Raw(Model.Description.Replace("\n", "<br>"))
                        </div>
                    }

                    @if (Model.EstimatedDuration.HasValue)
                    {
                        <div class="mb-3">
                            <strong><i class="fa fa-clock text-primary me-2"></i>Thời gian ước tính:</strong> @Model.EstimatedDuration phút
                        </div>
                    }

                    @if (!string.IsNullOrEmpty(Model.ServiceCategory))
                    {
                        <div class="mb-3">
                            <strong><i class="fa fa-tag text-primary me-2"></i>Danh mục:</strong> @Model.ServiceCategory
                        </div>
                    }
                </div>
            </div>

            <div class="col-lg-4">
                <div class="bg-light rounded p-4 mb-4">
                    <h4 class="mb-4">Thông Tin Dịch Vụ</h4>
                    
                    @if (Model.Price.HasValue)
                    {
                        <div class="mb-3">
                            <h3 class="text-primary mb-0">@Model.Price.Value.ToString("N0") đ</h3>
                        </div>
                    }
                    else
                    {
                        <div class="mb-3">
                            <h3 class="text-primary mb-0">Liên hệ</h3>
                        </div>
                    }

                    @if (Model.EstimatedDuration.HasValue)
                    {
                        <div class="mb-3">
                            <i class="fa fa-clock text-primary me-2"></i>
                            <strong>Thời gian:</strong> @Model.EstimatedDuration phút
                        </div>
                    }

                    <a href="@Url.Action("Index", "Booking", new { serviceId = Model.ServiceId })" class="btn btn-primary w-100 py-3">
                        <i class="fa fa-calendar-check me-2"></i>Đặt Lịch Ngay
                    </a>
                </div>

                @if (Model.IsFeatured)
                {
                    <div class="bg-primary text-white rounded p-4 text-center">
                        <i class="fa fa-star fa-3x mb-3"></i>
                        <h5>Dịch Vụ Nổi Bật</h5>
                        <p class="mb-0">Dịch vụ được khách hàng yêu thích nhất</p>
                    </div>
                }
            </div>
        </div>

        <!-- Reviews Section -->
        @await Html.PartialAsync("_ServiceReviews", Model)

        <!-- Related Services -->
        @if (relatedServices.Any())
        {
            <section class="related-services mt-5 pt-4">
                <div class="d-flex align-items-end justify-content-between mb-4">
                    <div>
                        <span class="text-primary text-uppercase fw-bold d-block mb-2" style="font-size: 11px; letter-spacing: 1.5px;">Cùng nhóm dịch vụ</span>
                        <h3 class="mb-0">Dịch Vụ Liên Quan</h3>
                    </div>
                    <a href="@Url.Action("Index", "Service")" class="btn-text-primary d-none d-md-inline-flex align-items-center">
                        Xem tất cả <i class="fa fa-arrow-right ms-2"></i>
                    </a>
                </div>

                <div class="related-service-grid">
                    @foreach (var service in relatedServices)
                    {
                        var serviceDetailsUrl = string.IsNullOrEmpty(service.Slug)
                            ? Url.Action("Details", "Service", new { id = service.ServiceId })
                            : Url.Action("Details", "Service", new { slug = service.Slug });

                        <article class="related-service-card wow fadeInUp" data-wow-delay="0.1s">
                            <a class="related-service-image" href="@serviceDetailsUrl">
                                @if (!string.IsNullOrEmpty(service.ImageUrl))
                                {
                                    <img src="@service.ImageUrl" alt="@service.ServiceName">
                                }
                                else
                                {
                                    <img src="~/img/service-1.jpg" alt="@service.ServiceName">
                                }
                                @if (!string.IsNullOrEmpty(service.ServiceCategory))
                                {
                                    <span>@service.ServiceCategory</span>
                                }
                            </a>

                            <div class="related-service-body">
                                <h5>
                                    <a href="@serviceDetailsUrl">@service.ServiceName</a>
                                </h5>
                                @if (!string.IsNullOrEmpty(service.ShortDescription))
                                {
                                    <p>@(service.ShortDescription.Length > 80 ? service.ShortDescription.Substring(0, 80) + "..." : service.ShortDescription)</p>
                                }

                                <div class="related-service-meta">
                                    <span><i class="fa fa-clock text-primary me-1"></i>@(service.EstimatedDuration.HasValue ? $"{service.EstimatedDuration} phút" : "Theo lịch hẹn")</span>
                                    <strong>@(service.Price.HasValue ? service.Price.Value.ToString("N0") + " đ" : "Liên hệ")</strong>
                                </div>

                                <div class="related-service-actions">
                                    <a class="btn btn-outline-dark" href="@serviceDetailsUrl">Chi tiết</a>
                                    <a class="btn btn-primary" href="@Url.Action("Index", "Booking", new { serviceId = service.ServiceId })">
                                        Đặt lịch <i class="fa fa-arrow-right ms-2"></i>
                                    </a>
                                </div>
                            </div>
                        </article>
                    }
                </div>
            </section>
        }
    </div>
</div>
<!-- Service Details End -->


